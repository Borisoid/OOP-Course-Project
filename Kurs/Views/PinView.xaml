<UserControl x:Class="Kurs.Views.PinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kurs.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100"
             Height="{Binding RectHeight}" Width="{Binding RectWidth}">
    <Canvas Height="{Binding RectHeight}" Width="{Binding RectWidth}"
            x:Name="canvas">
        <Line X1="{Binding LineX1}" Y1="{Binding LineY}" X2="{Binding LineX2}" Y2="{Binding LineY}"
              Canvas.Top="0"
              Canvas.Left="0"
              Stroke="Black"
              Width="5"/>
        <Ellipse Height="{Binding CircleRadius}" Width="{Binding CircleRadius}"
                 Canvas.Top="{Binding CircleTopPositionInCanvas}"
                 Canvas.Left="{Binding CircleLeftPositionInCanvas}"
                 Stroke="Black"
                 StrokeThickness="2"/>
    </Canvas>
</UserControl>
